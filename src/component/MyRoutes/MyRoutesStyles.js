import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    bottomNavigation: {
      width: '100%',
      position: 'fixed',
      bottom: 0,
    },
 });
export default useStyles;